services:
  - type: web
    name: notix-nexus-bot
    runtime: node
    plan: free
    buildCommand: npm install && npm run build --workspace=packages/bot
    startCommand: npm run start --workspace=packages/bot
    envVars:
      - key: NODE_ENV
        value: production
      - key: TOKEN
        sync: false
      - key: CLIENT_ID
        value: "1505996435899027586"
      - key: BOT_OWNER_ID
        value: "123456789"
